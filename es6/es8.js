let obj = { name: "dang" }

Object.values(obj)
Object.entries(obj)


// padStart
const message = "Dang"
const newMessage = message.padStart(15, "*").padEnd(20, "--")

Object.getOwnPropertyDescriptor(obj)

// async  await
